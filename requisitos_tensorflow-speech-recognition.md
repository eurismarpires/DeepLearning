### bibliotecas necessárias para rodar o código
https://github.com/pannous/tensorflow-speech-recognition

    $ pip install tflearn
    $ conda install -c bokeh pyaudio=0.2.7
    $ pip install -U scikit-image
    $ pip install h5py
    $ pip install librosa
